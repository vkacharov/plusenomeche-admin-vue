## Since we dropped the versioning and conflict resolution, 
## we have to manually check if there are Allocations referencing the Cause being deleted

#set( $query = {
  "expression": "#partitionKey = :partitionKey",
  "expressionNames": {
      "#partitionKey": "causeName"
  },
  "expressionValues": {
      ":partitionKey": $util.dynamodb.toDynamoDB($ctx.args.input.causeName)
  }
} )

{
    "operation": "Query",
    "query":     $util.toJson($query),
    "index": "byCause"
}