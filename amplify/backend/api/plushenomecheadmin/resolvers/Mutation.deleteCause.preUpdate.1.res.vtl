#if($ctx.error)
    $util.error($ctx.error.message, $ctx.error.type)
#end

#if(!$ctx.result.items.isEmpty())
	$util.error("Cannot delete Cause: It is referenced by existing Allocations")
#end

$util.toJson({})