#if($ctx.error)
    $util.error($ctx.error.message, $ctx.error.type)
#end

#if(!$ctx.result.items.isEmpty())
	$util.error("Cannot delete Allocation: It is referenced by existing Expenses")
#end

$util.toJson({})