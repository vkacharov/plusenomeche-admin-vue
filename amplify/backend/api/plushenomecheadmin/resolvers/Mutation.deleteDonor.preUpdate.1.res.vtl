#if($ctx.error)
    $util.error($ctx.error.message, $ctx.error.type)
#end

#if(!$ctx.result.items.isEmpty())
	$util.error("Cannot delete Donor: It is referenced by existing Donations")
#end

$util.toJson({})